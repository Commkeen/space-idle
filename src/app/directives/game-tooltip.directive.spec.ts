import { GameTooltipDirective } from './game-tooltip.directive';

describe('GameTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new GameTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
